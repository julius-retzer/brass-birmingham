---
phase: 01-game-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/board.ts
  - src/data/industryTiles.ts
  - src/data/merchants.ts
  - src/data/board.test.ts
  - src/data/industryTiles.test.ts
  - src/data/merchants.test.ts
autonomous: true
requirements: [ENGINE-01]

must_haves:
  truths:
    - "Every city has the correct number of industry slots matching real Brass Birmingham board"
    - "Every industry slot accepts the correct industry types per real Brass Birmingham board"
    - "Every connection has correct canal/rail era types per real Brass Birmingham board"
    - "Farm brewery locations exist and connect to adjacent cities"
    - "Every industry tile has correct cost, VP, resources, quantity, and link scoring values"
    - "2-player merchant setup excludes Warrington and Nottingham"
  artifacts:
    - path: "src/data/board.ts"
      provides: "Corrected city industry slots, connections, farm breweries"
      contains: "farmBreweryNorth"
    - path: "src/data/industryTiles.ts"
      provides: "Corrected tile definitions matching real game"
      contains: "pottery_5"
    - path: "src/data/merchants.ts"
      provides: "Merchant definitions with player count filtering"
      contains: "playerCount"
    - path: "src/data/board.test.ts"
      provides: "Data validation tests for board accuracy"
    - path: "src/data/industryTiles.test.ts"
      provides: "Data validation tests for tile accuracy"
    - path: "src/data/merchants.test.ts"
      provides: "Data validation tests for merchant filtering"
  key_links:
    - from: "src/data/board.ts"
      to: "src/store/gameStore.ts"
      via: "cityIndustrySlots import for build validation"
      pattern: "cityIndustrySlots"
    - from: "src/data/industryTiles.ts"
      to: "src/store/gameStore.ts"
      via: "industryTileDefinitions import for tile setup"
      pattern: "industryTileDefinitions"
---

<objective>
Fix all game data files (board.ts, industryTiles.ts, merchants.ts) to match the real Brass Birmingham board game.

Purpose: All action validation, scoring, and game logic depend on accurate data. The research phase found that almost every city's industry slots are wrong, multiple connections have wrong era types, farm breweries are missing, and many industry tile stats are incorrect. Fixing data first prevents cascading errors in all subsequent plans.

Output: Corrected data files with validation tests proving accuracy against the reference implementation.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-engine/01-CONTEXT.md
@.planning/phases/01-game-engine/01-RESEARCH.md

@src/data/board.ts
@src/data/industryTiles.ts
@src/data/merchants.ts
@src/data/cards.ts
@ai-docs/brass-birmingham-rules.mdc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix board data -- city industry slots, connections, farm breweries</name>
  <files>src/data/board.ts, src/data/board.test.ts</files>
  <read_first>
    - src/data/board.ts (current board data to see structure and types)
    - ai-docs/brass-birmingham-rules.mdc (rule authority for board layout)
    - .planning/phases/01-game-engine/01-RESEARCH.md (contains exact discrepancy tables)
  </read_first>
  <behavior>
    - Test: Birmingham has exactly 4 slots: [cotton,manufacturer], [manufacturer], [iron], [manufacturer]
    - Test: Coventry has exactly 3 slots: [pottery], [manufacturer,coal], [iron,manufacturer]
    - Test: Dudley has exactly 2 slots: [coal], [iron] (NO brewery slot)
    - Test: Wolverhampton has exactly 2 slots: [manufacturer], [manufacturer,coal]
    - Test: Walsall has exactly 2 slots: [iron,manufacturer], [manufacturer,brewery]
    - Test: Redditch has exactly 2 slots: [manufacturer,coal], [iron]
    - Test: Worcester has exactly 2 slots: [cotton], [cotton]
    - Test: Kidderminster has exactly 2 slots: [cotton,coal], [cotton]
    - Test: Cannock has exactly 2 slots: [manufacturer,coal], [coal]
    - Test: Tamworth has exactly 2 slots: [cotton,coal], [cotton,coal]
    - Test: Nuneaton has exactly 2 slots: [manufacturer,brewery], [cotton,coal]
    - Test: Coalbrookdale has exactly 3 slots: [iron,brewery], [iron], [coal]
    - Test: Stone has exactly 2 slots: [cotton,brewery], [manufacturer,coal]
    - Test: Stafford has exactly 2 slots: [manufacturer,brewery], [pottery]
    - Test: Stoke has exactly 3 slots: [cotton,manufacturer], [pottery,iron], [manufacturer]
    - Test: Leek has exactly 2 slots: [cotton,manufacturer], [cotton,coal]
    - Test: Uttoxeter has exactly 2 slots: [manufacturer,brewery], [cotton,brewery]
    - Test: Burton has exactly 2 slots: [manufacturer,coal], [brewery]
    - Test: Derby has exactly 3 slots: [cotton,brewery], [cotton,manufacturer], [iron]
    - Test: Belper has exactly 3 slots: [cotton,manufacturer], [coal], [pottery]
    - Test: farmBreweryNorth exists with 1 slot: [brewery]
    - Test: farmBrewerySouth exists with 1 slot: [brewery]
    - Test: Birmingham-Redditch connection is rail only
    - Test: Birmingham-Nuneaton connection is rail only
    - Test: Burton-Cannock connection is rail only
    - Test: Burton-Walsall connection is canal only
    - Test: Tamworth-Nuneaton connection has both canal and rail
    - Test: Tamworth-Walsall connection is rail only
    - Test: Coventry-Nuneaton connection is rail only
    - Test: Coventry-Oxford connection does NOT exist
    - Test: Belper-Nottingham connection has both canal and rail
    - Test: Gloucester-Oxford connection does NOT exist
    - Test: Redditch-Worcester connection does NOT exist
    - Test: Cannock-Walsall connection has both canal and rail
    - Test: farmBreweryNorth connects to Cannock and Walsall
    - Test: farmBrewerySouth connects to Kidderminster and Worcester
    - Test: total city count is 22 (20 cities + 2 farm breweries)
    - Test: total merchant count is 5
  </behavior>
  <action>
1. Create `src/data/board.test.ts` with all behavior tests listed above. Tests should import `cities`, `connections`, `cityIndustrySlots` from `./board` and assert exact values.

2. Update `src/data/board.ts`:

Add farm brewery cities to the `cities` object:
```typescript
farmBreweryNorth: { name: 'Farm Brewery (North)', type: 'city' },
farmBrewerySouth: { name: 'Farm Brewery (South)', type: 'city' },
```

Replace the entire `cityIndustrySlots` object with corrected values per the reference implementation (see RESEARCH.md discrepancy table). The corrected values for every city are:

| City | Corrected Slots |
|------|----------------|
| birmingham | [cotton,manufacturer], [manufacturer], [iron], [manufacturer] |
| coventry | [pottery], [manufacturer,coal], [iron,manufacturer] |
| dudley | [coal], [iron] |
| wolverhampton | [manufacturer], [manufacturer,coal] |
| walsall | [iron,manufacturer], [manufacturer,brewery] |
| redditch | [manufacturer,coal], [iron] |
| worcester | [cotton], [cotton] |
| kidderminster | [cotton,coal], [cotton] |
| cannock | [manufacturer,coal], [coal] |
| tamworth | [cotton,coal], [cotton,coal] |
| nuneaton | [manufacturer,brewery], [cotton,coal] |
| coalbrookdale | [iron,brewery], [iron], [coal] |
| stone | [cotton,brewery], [manufacturer,coal] |
| stafford | [manufacturer,brewery], [pottery] |
| stoke | [cotton,manufacturer], [pottery,iron], [manufacturer] |
| leek | [cotton,manufacturer], [cotton,coal] |
| uttoxeter | [manufacturer,brewery], [cotton,brewery] |
| burton | [manufacturer,coal], [brewery] |
| derby | [cotton,brewery], [cotton,manufacturer], [iron] |
| belper | [cotton,manufacturer], [coal], [pottery] |
| farmBreweryNorth | [brewery] |
| farmBrewerySouth | [brewery] |

Add farm brewery slots to `cityIndustrySlots`:
```typescript
farmBreweryNorth: [['brewery']],
farmBrewerySouth: [['brewery']],
```

Fix connections array. Remove these wrong connections:
- `{ from: 'coventry', to: 'oxford' }` -- does not exist in real game
- `{ from: 'gloucester', to: 'oxford' }` -- does not exist in real game
- `{ from: 'redditch', to: 'worcester' }` -- does not exist in real game

Change these connection types:
- birmingham-redditch: `['rail']` (was `['canal', 'rail']`)
- birmingham-nuneaton: `['rail']` (was `['canal', 'rail']`)
- burton-cannock: `['rail']` (was `['canal', 'rail']`)
- tamworth-walsall: `['rail']` (was `['canal', 'rail']`)
- coventry-nuneaton: `['rail']` (was `['canal', 'rail']`)
- belper-nottingham: `['canal', 'rail']` (was `['rail']`)
- tamworth-nuneaton: `['canal', 'rail']` (was `['rail']`)

Add these missing connections:
- `{ from: 'burton', to: 'walsall', types: ['canal'] }` -- canal only
- `{ from: 'cannock', to: 'walsall', types: ['canal', 'rail'] }` -- both eras
- `{ from: 'farmBreweryNorth', to: 'cannock', types: ['canal', 'rail'] }`
- `{ from: 'farmBreweryNorth', to: 'walsall', types: ['canal', 'rail'] }`
- `{ from: 'farmBrewerySouth', to: 'kidderminster', types: ['canal', 'rail'] }`
- `{ from: 'farmBrewerySouth', to: 'worcester', types: ['canal', 'rail'] }`

Remove the duplicate connection `{ from: 'wolverhampton', to: 'dudley' }` (already exists as `{ from: 'dudley', to: 'wolverhampton' }`).

3. Run tests: `pnpm test --run src/data/board.test.ts`
  </action>
  <verify>
    <automated>pnpm test --run src/data/board.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/board.test.ts exists and contains `describe('board data'`
    - src/data/board.ts contains `farmBreweryNorth`
    - src/data/board.ts contains `farmBrewerySouth`
    - src/data/board.ts cityIndustrySlots.birmingham has exactly 4 entries
    - src/data/board.ts does NOT contain a connection from coventry to oxford
    - src/data/board.ts contains `{ from: 'burton', to: 'walsall'`
    - `pnpm test --run src/data/board.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 20 cities + 2 farm breweries have correct industry slots. All connections have correct era types. Farm breweries connect to adjacent cities. Board test file validates every value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix industry tile definitions to match real game</name>
  <files>src/data/industryTiles.ts, src/data/industryTiles.test.ts</files>
  <read_first>
    - src/data/industryTiles.ts (current tile data and IndustryTile interface)
    - ai-docs/brass-birmingham-rules.mdc (rule authority for tile stats)
    - .planning/phases/01-game-engine/01-RESEARCH.md (contains exact discrepancy list per industry type)
  </read_first>
  <behavior>
    - Test: Cotton level 1 quantity is 3, linkScoringIcons is 1
    - Test: Cotton level 2 quantity is 2 (not 3), linkScoringIcons is 2 (not 1)
    - Test: Cotton level 3 quantity is 3
    - Test: Cotton level 4 quantity is 3 (not 2)
    - Test: Coal level 1 quantity is 1 (not 2), linkScoringIcons is 2 (not 1)
    - Test: Coal level 2 quantity is 2 (not 3)
    - Test: Coal level 3 quantity is 2 (not 3)
    - Test: Coal level 4 quantity is 2 (not 4)
    - Test: Iron level 1 ironProduced is 4, quantity is 1
    - Test: Iron level 2 ironProduced is 4 (not 5), quantity is 1
    - Test: Iron level 3 ironProduced is 5 (not 6), incomeAdvancement is 2 (not 5), quantity is 1
    - Test: Iron level 4 ironProduced is 6 (not 7), incomeAdvancement is 1 (not 6), quantity is 1
    - Test: Manufacturer level 1 coalRequired is 1 (not 0), quantity is 1 (not 8), linkScoringIcons is 2 (not 1)
    - Test: Manufacturer level 2 coalRequired is 0 (not 1), ironRequired is 1 (not 0), quantity is 2 (not 7), linkScoringIcons is 1
    - Test: Manufacturer level 3 cost is 12, VP is 4, coalRequired is 2, ironRequired is 0 (not 1), beerRequired is 0 (not 1), linkScoringIcons is 0 (not 1), incomeAdvancement is 4 (not 2), quantity is 1 (not 5)
    - Test: Manufacturer level 4 cost is 8 (not 16), VP is 3, coalRequired is 0 (not 1), ironRequired is 1, beerRequired is 1, linkScoringIcons is 1, incomeAdvancement is 6 (not 3), quantity is 1 (not 4)
    - Test: Manufacturer level 5 cost is 16 (not 20), VP is 8, coalRequired is 1, ironRequired is 0 (not 1), beerRequired is 2 (not 1), linkScoringIcons is 2, incomeAdvancement is 2, quantity is 2 (not 3)
    - Test: Manufacturer level 6 cost is 20 (not 16), VP is 7, coalRequired is 0 (not 1), ironRequired is 0 (not 1), beerRequired is 1, linkScoringIcons is 1 (not 2), incomeAdvancement is 6, quantity is 1 (not 2)
    - Test: Manufacturer level 7 cost is 16 (not 20), VP is 9, coalRequired is 1, ironRequired is 1, beerRequired is 0 (not 1), linkScoringIcons is 0 (not 2), incomeAdvancement is 4, quantity is 1
    - Test: Manufacturer level 8 cost is 20, VP is 11, coalRequired is 0 (not 1), ironRequired is 2 (not 1), beerRequired is 1, linkScoringIcons is 1 (not 2), incomeAdvancement is 1, quantity is 2 (not 1)
    - Test: Pottery level 1 cost is 17 (not 5), VP is 10, coalRequired is 0 (not 1), ironRequired is 1 (not 0), hasLightbulbIcon is true
    - Test: Pottery level 2 cost is 0, VP is 1, quantity is 1 (not 2), hasLightbulbIcon is false
    - Test: Pottery level 3 cost is 22 (not 11), VP is 11 (not 5), coalRequired is 2 (not 1), beerRequired is 2 (not 1), hasLightbulbIcon is true (not false)
    - Test: Pottery level 4 cost is 0 (not 17), VP is 1 (not 11), hasLightbulbIcon is false
    - Test: Pottery level 5 exists with cost 24, VP 20, coalRequired 2, beerRequired 2, ironRequired 0, hasLightbulbIcon true, canBuildInCanalEra false, canBuildInRailEra true, quantity 1
    - Test: Brewery levels 1-3 all have ironRequired of 1 (not 0)
    - Test: Brewery level 1 linkScoringIcons is 2 (not 1)
    - Test: Brewery level 2 linkScoringIcons is 2 (not 1), quantity is 2 (not 1)
    - Test: Brewery level 3 linkScoringIcons is 2 (not 1), quantity is 2 (not 1)
    - Test: Brewery level 4 canBuildInCanalEra is false (rail only), linkScoringIcons is 2 (not 1)
    - Test: Total tiles per player for each industry type: cotton=11, coal=7, iron=4, manufacturer=11, pottery=5, brewery=7
  </behavior>
  <action>
1. Create `src/data/industryTiles.test.ts` with all behavior tests. Import `industryTileDefinitions` from `./industryTiles` and assert exact values for every field of every level of every industry.

2. Update `src/data/industryTiles.ts` with corrected values. The EXACT corrected values by industry type (verified against npow/brass-birmingham reference implementation):

**Cotton (total 11 tiles per player):**
- Level 1: UNCHANGED (cost 12, VP 5, coal 0, iron 0, beer 1, linkIcons 1, income 5, qty 3)
- Level 2: `quantity: 2` (was 3), `linkScoringIcons: 2` (was 1)
- Level 3: `quantity: 3` (was 2)
- Level 4: `quantity: 3` (was 2)

**Coal (total 7 tiles per player):**
- Level 1: `quantity: 1` (was 2), `linkScoringIcons: 2` (was 1)
- Level 2: `quantity: 2` (was 3)
- Level 3: `quantity: 2` (was 3)
- Level 4: `quantity: 2` (was 4)

**Iron (total 4 tiles per player):**
- Level 1: UNCHANGED (cost 5, VP 3, coal 1, ironProduced 4, linkIcons 1, income 3, qty 1)
- Level 2: `ironProduced: 4` (was 5)
- Level 3: `ironProduced: 5` (was 6), `incomeSpaces: 2` (was 5), `incomeAdvancement: 2` (was 5)
- Level 4: `ironProduced: 6` (was 7), `incomeSpaces: 1` (was 6), `incomeAdvancement: 1` (was 6)

**Manufacturer (total 11 tiles per player) -- ALL levels have changes:**
- Level 1: `coalRequired: 1` (was 0), `linkScoringIcons: 2` (was 1), `quantity: 1` (was 8)
- Level 2: `coalRequired: 0` (was 1), `ironRequired: 1` (was 0), `quantity: 2` (was 7)
- Level 3: `coalRequired: 2` (was 1), `ironRequired: 0` (was 1), `beerRequired: 0` (was 1), `linkScoringIcons: 0` (was 1), `incomeSpaces: 4` (was 2), `incomeAdvancement: 4` (was 2), `quantity: 1` (was 5)
- Level 4: `cost: 8` (was 16), `coalRequired: 0` (was 1), `incomeSpaces: 6` (was 3), `incomeAdvancement: 6` (was 3), `quantity: 1` (was 4)
- Level 5: `cost: 16` (was 20), `coalRequired: 1` (was 1), `ironRequired: 0` (was 1), `beerRequired: 2` (was 1), `quantity: 2` (was 3)
- Level 6: `cost: 20` (was 16), `coalRequired: 0` (was 1), `ironRequired: 0` (was 1), `linkScoringIcons: 1` (was 2), `quantity: 1` (was 2)
- Level 7: `cost: 16` (was 20), `beerRequired: 0` (was 1), `linkScoringIcons: 0` (was 2)
- Level 8: `coalRequired: 0` (was 1), `ironRequired: 2` (was 1), `linkScoringIcons: 1` (was 2), `quantity: 2` (was 1)

**Pottery (total 5 tiles per player) -- new level 5, lightbulb icon corrections:**
- Level 1: `cost: 17` (was 5), `coalRequired: 0` (was 1), `ironRequired: 1` (was 0). Already has `hasLightbulbIcon: true` (correct).
- Level 2: `quantity: 1` (was 2). Keep `hasLightbulbIcon: false` (correct -- level 2 CAN be developed).
- Level 3: `cost: 22` (was 11), `victoryPoints: 11` (was 5), `coalRequired: 2` (was 1), `beerRequired: 2` (was 1), `incomeSpaces: 5` (was 3), `incomeAdvancement: 5` (was 3), `hasLightbulbIcon: true` (was false -- level 3 canNOT be developed).
- Level 4: `cost: 0` (was 17), `victoryPoints: 1` (was 11), `incomeSpaces: 1` (was 5), `incomeAdvancement: 1` (was 5). Keep `hasLightbulbIcon: false` (correct -- level 4 CAN be developed).
- Level 5: ADD new entry `pottery_5` with: `level: 5, cost: 24, victoryPoints: 20, coalRequired: 2, beerRequired: 2, ironRequired: 0, linkScoringIcons: 1, quantity: 1, canBuildInCanalEra: false, canBuildInRailEra: true, hasLightbulbIcon: true, incomeSpaces: 5, incomeAdvancement: 5`

**Brewery (total 7 tiles per player):**
- Level 1: `ironRequired: 1` (was 0), `linkScoringIcons: 2` (was 1)
- Level 2: `ironRequired: 1` (was 0), `linkScoringIcons: 2` (was 1), `quantity: 2` (was 1)
- Level 3: `ironRequired: 1` (was 0), `linkScoringIcons: 2` (was 1), `quantity: 2` (was 1)
- Level 4: `canBuildInCanalEra: false` (was true -- brewery level 4 is rail-only), `linkScoringIcons: 2` (was 1)

3. Run tests: `pnpm test --run src/data/industryTiles.test.ts`
  </action>
  <verify>
    <automated>pnpm test --run src/data/industryTiles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/industryTiles.test.ts exists and contains `describe('industry tile definitions'`
    - src/data/industryTiles.ts contains `pottery_5` entry
    - src/data/industryTiles.ts pottery level 1 has `cost: 17`
    - src/data/industryTiles.ts brewery level 1 has `ironRequired: 1`
    - src/data/industryTiles.ts manufacturer level 1 has `quantity: 1`
    - src/data/industryTiles.ts manufacturer level 3 has `coalRequired: 2`
    - src/data/industryTiles.ts manufacturer level 4 has `cost: 8`
    - `pnpm test --run src/data/industryTiles.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All industry tile definitions match the real Brass Birmingham game. Cotton, coal, iron, manufacturer (all 8 levels corrected), pottery (including new level 5 and corrected lightbulb icons on levels 1/3/5), and brewery tiles all have correct cost, VP, resources, quantity, and link scoring values. Test file validates every corrected value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix merchant setup for 2-player games with test coverage</name>
  <files>src/data/merchants.ts, src/data/merchants.test.ts</files>
  <read_first>
    - src/data/merchants.ts (current merchant definitions)
    - ai-docs/brass-birmingham-rules.mdc (2-player merchant rules)
    - src/store/gameStore.ts (search for merchant setup logic to see how merchants are initialized)
  </read_first>
  <behavior>
    - Test: getMerchantsForPlayerCount(2) excludes warrington and nottingham
    - Test: getMerchantsForPlayerCount(2) includes shrewsbury, gloucester, oxford
    - Test: getMerchantsForPlayerCount(3) excludes nottingham but includes warrington
    - Test: getMerchantsForPlayerCount(4) includes all 5 merchants
    - Test: All merchants accept cotton, manufacturer, and pottery
  </behavior>
  <action>
1. Create `src/data/merchants.test.ts` with all behavior tests listed above. Import `merchants` and `getMerchantsForPlayerCount` from `./merchants` and assert exact filtering behavior.

2. Add a utility function `getMerchantsForPlayerCount` to `src/data/merchants.ts` that filters merchants by player count:

```typescript
export function getMerchantsForPlayerCount(playerCount: number): Record<string, Merchant> {
  if (playerCount <= 2) {
    // Per rules: "In a 2 player game, no Merchant tiles are placed in Warrington & Nottingham"
    const { warrington, nottingham, ...activeMerchants } = merchants
    return activeMerchants
  }
  if (playerCount === 3) {
    // Per rules: "In a 3-player game, no Merchant tiles are placed in Nottingham"
    const { nottingham, ...activeMerchants } = merchants
    return activeMerchants
  }
  return merchants
}
```

3. Verify each merchant has correct bonus type and industries accepted. Per rules, all merchant locations accept cotton, manufacturer, and pottery (already correct in current code).

4. Run all data tests: `pnpm test --run src/data/merchants.test.ts src/data/board.test.ts src/data/industryTiles.test.ts`

If any existing tests fail due to the data changes (expected -- tests may reference old slot values or connection types), note which tests fail but do NOT fix them yet. The subsequent plans handle test fixes. The important thing is that the data files are correct.
  </action>
  <verify>
    <automated>pnpm test --run src/data/merchants.test.ts src/data/board.test.ts src/data/industryTiles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/merchants.test.ts exists and tests getMerchantsForPlayerCount
    - src/data/merchants.ts contains `getMerchantsForPlayerCount`
    - src/data/merchants.ts contains `playerCount` parameter
    - src/data/merchants.ts contains `warrington, nottingham, ...activeMerchants`
    - All data validation tests pass (board.test.ts, industryTiles.test.ts, merchants.test.ts)
  </acceptance_criteria>
  <done>Merchant setup supports 2-player and 3-player filtering with test coverage. All data files (board.ts, industryTiles.ts, merchants.ts) are corrected and validated. Data validation tests confirm accuracy.</done>
</task>

</tasks>

<verification>
- `pnpm test --run src/data/board.test.ts` -- all board data tests pass
- `pnpm test --run src/data/industryTiles.test.ts` -- all tile data tests pass
- `pnpm test --run src/data/merchants.test.ts` -- all merchant tests pass
- `grep -c 'farmBreweryNorth' src/data/board.ts` returns >= 1
- `grep -c 'pottery_5' src/data/industryTiles.ts` returns >= 1
- `grep -c 'getMerchantsForPlayerCount' src/data/merchants.ts` returns >= 1
</verification>

<success_criteria>
All game data files match the real Brass Birmingham board game. Every city has correct industry slots, every connection has correct era types, farm breweries exist, all industry tiles have correct stats, and merchant setup supports 2-player games. Validation tests prove data accuracy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-01-SUMMARY.md`
</output>
