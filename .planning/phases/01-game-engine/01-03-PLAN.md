---
phase: 01-game-engine
plan: 03
type: tdd
wave: 3
depends_on: [01, 02]
files_modified:
  - src/store/gameStore.ts
  - src/store/gameStore.network.test.ts
  - src/store/gameStore.develop.test.ts
  - src/store/gameStore.sell.test.ts
  - src/store/gameStore.build.test.ts
  - src/store/gameStore.turns.test.ts
  - src/store/gameStore.error.test.ts
  - src/store/gameStore.markets.test.ts
autonomous: true
requirements: [ENGINE-05, ENGINE-07, ENGINE-08, ENGINE-09]

must_haves:
  truths:
    - "Network action validates adjacency requirement correctly (fix 2 failing tests)"
    - "Network action costs vary by era (canal=free or coal, rail=coal+beer per rules)"
    - "Brewery beer production is era-dependent (canal=1, rail=2)"
    - "Develop action unskips all 3 skipped tests and they pass"
    - "Bankruptcy/income shortfall is handled per rules"
    - "Empty market fallback prices work correctly"
    - "Turn order determined by lowest spending each round"
    - "Player switches correctly after each action (2 per turn, 1 in first round)"
  artifacts:
    - path: "src/store/gameStore.ts"
      provides: "Fixed action validation for network, build, develop, sell"
    - path: "src/store/gameStore.network.test.ts"
      provides: "Fixed and passing network tests (0 failing, 0 skipped)"
    - path: "src/store/gameStore.develop.test.ts"
      provides: "Fixed and passing develop tests (0 skipped)"
  key_links:
    - from: "src/store/gameStore.ts"
      to: "src/data/board.ts connections"
      via: "Network action checks adjacency against connections array"
      pattern: "connections"
    - from: "src/store/gameStore.ts executeBuildAction"
      to: "era check for brewery"
      via: "Brewery beer production doubles in rail era"
      pattern: "beerProduced"
---

<objective>
Fix all action validation gaps, unskip failing/skipped tests, and handle edge cases for the 7 action types.

Purpose: Research identified 2 failing tests (network adjacency, costs vary by era), 6 skipped tests (3 develop, 1 error, others), and gaps in brewery beer production (era-dependent), overbuilding rules, and sell action. This plan fills those gaps with TDD.

Output: All 7 action types correctly enforce Brass Birmingham rules. Zero failing tests, zero skipped tests for action-related test files.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-engine/01-CONTEXT.md
@.planning/phases/01-game-engine/01-RESEARCH.md
@.planning/phases/01-game-engine/01-01-SUMMARY.md
@.planning/phases/01-game-engine/01-02-SUMMARY.md

@src/store/gameStore.ts
@src/data/board.ts
@src/data/industryTiles.ts
@ai-docs/brass-birmingham-rules.mdc
</context>

<feature>
  <name>Action Validation Fixes and Edge Cases</name>
  <files>src/store/gameStore.ts, src/store/gameStore.network.test.ts, src/store/gameStore.develop.test.ts, src/store/gameStore.sell.test.ts, src/store/gameStore.build.test.ts, src/store/gameStore.turns.test.ts, src/store/gameStore.error.test.ts, src/store/gameStore.markets.test.ts</files>
  <behavior>
    Network Action Fixes:
    - Test: Network action requires adjacency to an existing network or industry (currently failing)
    - Test: Canal era link building is free (no coal cost)
    - Test: Rail era link building requires coal (1 coal per link, and rail links cost money per tile level)
    - Test: Cannot build a canal link in rail era
    - Test: Cannot build a rail link in canal era
    - Test: Cannot build on an already-occupied link

    Brewery Beer Production:
    - Test: Building brewery in canal era places 1 beer barrel on tile
    - Test: Building brewery in rail era places 2 beer barrels on tile
    - Test: Beer barrel count comes from tile.beerProduced * (era === 'rail' ? 2 : 1)

    Develop Action Fixes:
    - Test: Unskip and fix all 3 skipped develop tests
    - Test: Cannot develop pottery tiles with lightbulb icon (hasLightbulbIcon=true)
    - Test: Develop removes 1-2 lowest level tiles from player mat, costs 1 iron per tile

    Sell Action:
    - Test: Selling flips the industry tile
    - Test: Selling requires beer (from own brewery or connected brewery)
    - Test: Merchant bonus is applied correctly (money, VP, income, develop)

    Edge Cases:
    - Test: Bankruptcy -- when player cannot pay loan income penalty, income drops to -10
    - Test: Empty coal market -- price is 1 pound per coal from market when cubes are depleted (fallback price mechanism)
    - Test: Empty iron market -- price is 1 pound per iron from market when cubes are depleted

    Turn Order:
    - Test: Turn order re-determined each round based on lowest total spending
    - Test: Player with least spending goes first next round
    - Test: Ties in spending preserve previous turn order

    Player Switching:
    - Test: After each action, actionsRemaining decrements by 1
    - Test: When actionsRemaining reaches 0, switch to next player in turn order
    - Test: First round of each era sets actionsRemaining to 1 (not 2)
  </behavior>
  <implementation>
1. Read ALL existing test files listed in <files> to understand current test structure, what's failing, and what's skipped. Search for `it.skip`, `test.skip`, `describe.skip`, and `it.todo` patterns.

2. Fix network action in gameStore.ts:
   - Fix adjacency check: a network link can only be built from a city where the player has an existing industry OR an existing link. Read the exact guard logic (likely `canBuildLink` or similar) and fix the adjacency requirement.
   - Fix era-dependent costs: canal links should be free (or cost varies per rules), rail links require coal. Read the current cost logic and correct it per Brass Birmingham rules.
   - Update the 2 failing network tests to pass.

3. Fix brewery beer production in gameStore.ts:
   - In the build action execution (likely `executeBuildAction` or similar), when placing a brewery tile, calculate beer to place as: `context.era === 'rail' ? tile.beerProduced * 2 : tile.beerProduced`
   - Add test for this in gameStore.build.test.ts

4. Fix develop action:
   - Read the 3 skipped develop tests to understand what they expect
   - Fix the develop action logic in gameStore.ts to match expected behavior
   - Unskip all 3 tests and verify they pass

5. Fix/verify sell action:
   - Verify sell correctly flips tile, requires beer, applies merchant bonuses
   - Add tests for any missing sell behaviors

6. Fix edge cases:
   - Verify bankruptcy handling (income cannot drop below -10)
   - Verify empty market fallback prices
   - Add missing edge case tests in gameStore.markets.test.ts and gameStore.error.test.ts
   - Unskip the 1 skipped error test

7. Verify turn order and player switching:
   - Add/fix tests in gameStore.turns.test.ts for spending-based turn order
   - Verify actionsRemaining logic

8. Run full test suite: `pnpm test --run`
  </implementation>
</feature>

<verification>
- `pnpm test --run src/store/gameStore.network.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.develop.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.sell.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.build.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.turns.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.error.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.markets.test.ts` -- 0 failing, 0 skipped
- `grep -c 'it.skip\|test.skip\|describe.skip' src/store/gameStore.*.test.ts` returns 0 for all action test files
</verification>

<success_criteria>
All 7 action types correctly enforce Brass Birmingham rules for 2 players. Network adjacency and era-dependent costs work. Brewery beer production doubles in rail era. Develop action handles tile removal with iron cost. Edge cases (bankruptcy, empty markets) are handled. Turn order is spending-based. All action test files have 0 failing and 0 skipped tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-03-SUMMARY.md`
</output>
