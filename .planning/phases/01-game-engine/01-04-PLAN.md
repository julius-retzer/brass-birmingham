---
phase: 01-game-engine
plan: 04
type: tdd
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/store/gameStore.integration.test.ts
  - src/store/gameStore.ts
autonomous: true
requirements: [ENGINE-06, ENGINE-01]

must_haves:
  truths:
    - "A full 2-player game can be simulated programmatically from start through final scoring without errors"
    - "Game progresses: setup -> Canal era actions -> Canal scoring -> era transition -> Rail era actions -> Rail scoring -> game over"
    - "Final VP totals are verifiable against hand-calculated expected values"
    - "All test files pass with zero failures and zero skipped tests"
    - "Test coverage is measured and reported"
  artifacts:
    - path: "src/store/gameStore.integration.test.ts"
      provides: "Complete full-game simulation test"
      contains: "game over"
    - path: "src/store/gameStore.ts"
      provides: "Any final fixes discovered during integration testing"
  key_links:
    - from: "src/store/gameStore.integration.test.ts"
      to: "src/store/gameStore.ts"
      via: "Creates actor, sends events for complete game flow"
      pattern: "createActor"
    - from: "integration test"
      to: "automatic era transitions"
      via: "Verifies era auto-transitions during game flow"
      pattern: "eraScoring"
---

<objective>
Create the full game integration test and achieve comprehensive test coverage.

Purpose: The integration test is the ultimate proof the engine works (Success Criteria #1). It simulates a complete 2-player game programmatically from setup through Canal era, scoring, era transition, Rail era, final scoring, and game over. This catches any remaining issues in the interaction between all game systems. Also closes ENGINE-06 (100% coverage target).

Output: Passing integration test proving a complete game works end-to-end. All test files green, coverage measured.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-engine/01-CONTEXT.md
@.planning/phases/01-game-engine/01-RESEARCH.md
@.planning/phases/01-game-engine/01-01-SUMMARY.md
@.planning/phases/01-game-engine/01-02-SUMMARY.md
@.planning/phases/01-game-engine/01-03-SUMMARY.md

@src/store/gameStore.ts
@src/store/gameStore.integration.test.ts
@ai-docs/brass-birmingham-rules.mdc
</context>

<feature>
  <name>Full Game Integration Test and Coverage</name>
  <files>src/store/gameStore.integration.test.ts, src/store/gameStore.ts</files>
  <behavior>
    Full Game Simulation:
    - Test: Create a 2-player game with known seed/state
    - Test: Canal era -- each player takes actions (build, network, sell, develop, loan, scout, pass) using valid cards and locations
    - Test: Canal era ends automatically when draw pile and hands are empty
    - Test: Canal scoring awards correct VP (link + industry)
    - Test: Era transition removes level 1 tiles, re-deals 8 cards each, sets era to 'rail'
    - Test: Rail era -- each player takes actions across multiple rounds
    - Test: Rail era ends automatically when draw pile and hands are empty
    - Test: Rail scoring awards correct VP
    - Test: Game reaches 'gameOver' final state
    - Test: Winner determined correctly (highest VP, tiebreak by income, then money)

    Hand-Calculated Reference:
    - Set up a specific game state using TEST_ events with known industries, links, and scores
    - Calculate expected VP by hand for both canal and rail scoring
    - Assert game produces those exact VP totals

    Coverage:
    - All 18 test files pass with 0 failures, 0 skipped
    - Run `pnpm test --run --coverage` to measure coverage
  </behavior>
  <implementation>
1. Read the existing `src/store/gameStore.integration.test.ts` to understand what's already written and what's skipped.

2. Design a deterministic game scenario:
   - Use TEST_SET_PLAYER_HAND to give each player specific known cards
   - Use TEST_SET_DRAW_PILE to set a known draw pile
   - Play through a sequence of ~20-30 actions per era covering all 7 action types
   - Use simple, verifiable moves (e.g., build cotton in Birmingham, build link between Birmingham and Dudley)

3. Hand-calculate expected VP for the scenario:
   - Canal era: count each player's flipped industries VP + link VP (sum linkScoringIcons on adjacent flipped industries)
   - Rail era: same calculation on rail-era industries and links
   - Record expected VP in test comments for auditability

4. Write the integration test:
   - Create actor with `createActor(brassBirminghamMachine, { input: { playerNames: ['Alice', 'Bob'] } })`
   - Start actor
   - Set up deterministic state using TEST_ events
   - Send action events in sequence (SELECT_CARD, SELECT_LOCATION, CONFIRM_ACTION, etc.)
   - After each action, assert state is valid (no errors, correct player turn)
   - Verify automatic era transition occurs (state passes through eraScoring -> eraTransition -> action)
   - Verify final state is gameOver
   - Assert final VP matches hand-calculated values

5. If the integration test reveals bugs in gameStore.ts, fix them. These will be small issues missed by unit tests (interaction bugs, state inconsistencies).

6. Run full test suite with coverage:
   ```
   pnpm test --run --coverage
   ```
   Review coverage output. If major gaps exist (untested branches/functions), add targeted unit tests in the most appropriate existing test file.

7. Final check: `pnpm test --run` -- all tests pass, 0 failures, 0 skipped.
  </implementation>
</feature>

<verification>
- `pnpm test --run src/store/gameStore.integration.test.ts` -- integration test passes
- `pnpm test --run` -- ALL tests pass (0 failures, 0 skipped across all 18 test files)
- `grep -c 'it.skip\|test.skip\|describe.skip' src/store/gameStore.*.test.ts` returns 0 total
- `grep 'gameOver\|game over' src/store/gameStore.integration.test.ts` confirms game reaches final state
- `pnpm test --run --coverage` runs and reports coverage metrics
</verification>

<success_criteria>
A complete 2-player game simulation runs from start to final scoring without errors. Canal and rail era scoring produce hand-verified VP totals. Automatic era transitions trigger correctly during gameplay. All 18 test files pass with 0 failures and 0 skipped tests. Coverage metrics are measured and available.
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-04-SUMMARY.md`
</output>
