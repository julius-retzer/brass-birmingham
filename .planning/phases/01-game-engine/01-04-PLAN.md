---
phase: 01-game-engine
plan: 04
type: tdd
wave: 3
depends_on: [01, 02]
files_modified:
  - src/store/gameStore.ts
  - src/store/gameStore.turns.test.ts
  - src/store/gameStore.error.test.ts
  - src/store/gameStore.markets.test.ts
autonomous: true
requirements: [ENGINE-07, ENGINE-08, ENGINE-09]

must_haves:
  truths:
    - "Bankruptcy/income shortfall is handled per rules (remove tiles for half cost, lose 1 VP per pound shortfall)"
    - "Empty market fallback prices work correctly (coal at 8 per piece, iron at 6 per piece)"
    - "Turn order determined by lowest spending each round"
    - "Player switches correctly after each action (2 per turn, 1 in first round)"
  artifacts:
    - path: "src/store/gameStore.ts"
      provides: "Fixed edge case handling for bankruptcy, markets, turn order"
    - path: "src/store/gameStore.turns.test.ts"
      provides: "Turn order and player switching tests (0 failing, 0 skipped)"
    - path: "src/store/gameStore.error.test.ts"
      provides: "Error/bankruptcy tests (0 failing, 0 skipped)"
    - path: "src/store/gameStore.markets.test.ts"
      provides: "Market edge case tests (0 failing, 0 skipped)"
  key_links:
    - from: "src/store/gameStore.ts"
      to: "turn order logic"
      via: "Spending-based turn order recalculation at end of round"
      pattern: "spending"
    - from: "src/store/gameStore.ts"
      to: "market fallback"
      via: "Empty market coal price 8, iron price 6"
      pattern: "marketPrice"
---

<objective>
Fix edge cases (bankruptcy, empty markets) and turn management (turn order, player switching).

Purpose: Research identified 1 skipped error test and gaps in bankruptcy handling, empty market fallback prices, and turn order verification. These edge cases are critical for a correct 2-player game simulation. This plan runs in parallel with Plan 01-03 (action validation) since they touch different behavioral areas.

Output: Bankruptcy, empty markets, turn order, and player switching all enforce Brass Birmingham rules correctly. Zero failing or skipped tests.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-engine/01-CONTEXT.md
@.planning/phases/01-game-engine/01-RESEARCH.md
@.planning/phases/01-game-engine/01-01-SUMMARY.md
@.planning/phases/01-game-engine/01-02-SUMMARY.md

@src/store/gameStore.ts
@ai-docs/brass-birmingham-rules.mdc
</context>

<feature>
  <name>Edge Cases and Turn Management</name>
  <files>src/store/gameStore.ts, src/store/gameStore.turns.test.ts, src/store/gameStore.error.test.ts, src/store/gameStore.markets.test.ts</files>
  <behavior>
    Bankruptcy / Income Shortfall:
    - Test: When player cannot pay negative income, must remove industry tiles worth half cost (rounded down) to cover shortfall
    - Test: If player has no tiles to remove and still has shortfall, lose 1 VP per pound short
    - Test: Income level cannot drop below -10 (loan restriction)
    - Test: Unskip the 1 skipped error test and verify it passes

    Empty Markets:
    - Test: When coal market is empty, coal can still be purchased for 8 pounds per piece
    - Test: When iron market is empty, iron can still be purchased for 6 pounds per piece
    - Test: Market purchase requires connection to merchant space (for coal) but not for iron

    Turn Order:
    - Test: Turn order re-determined each round based on lowest total spending
    - Test: Player with least spending goes first next round
    - Test: Ties in spending preserve previous turn order

    Player Switching:
    - Test: After each action, actionsRemaining decrements by 1
    - Test: When actionsRemaining reaches 0, switch to next player in turn order
    - Test: First round of each era sets actionsRemaining to 1 (not 2)
  </behavior>
  <implementation>
1. Read ALL existing test files listed in <files> to understand current test structure, what's failing, and what's skipped. Search for `it.skip`, `test.skip`, `describe.skip`, and `it.todo` patterns.

2. Fix bankruptcy/income shortfall in gameStore.ts:
   - Verify the income collection logic handles negative income
   - When player can't pay negative income: remove own industry tiles from board (each worth half its cost rounded down) until enough money
   - If still short after removing all removable tiles: lose 1 VP per pound shortfall
   - Unskip the 1 skipped error test

3. Fix/verify empty market fallback prices:
   - When coal market is depleted: coal costs 8 pounds per piece (per rules)
   - When iron market is depleted: iron costs 6 pounds per piece (per rules)
   - Add tests in gameStore.markets.test.ts for these edge cases

4. Verify turn order and player switching:
   - Turn order recalculation at end of each round based on spending
   - Verify actionsRemaining logic (2 per turn normally, 1 in first round of each era)
   - Add/fix tests in gameStore.turns.test.ts

5. Run edge case test suite: `pnpm test --run src/store/gameStore.turns.test.ts src/store/gameStore.error.test.ts src/store/gameStore.markets.test.ts`
  </implementation>
</feature>

<verification>
- `pnpm test --run src/store/gameStore.turns.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.error.test.ts` -- 0 failing, 0 skipped
- `pnpm test --run src/store/gameStore.markets.test.ts` -- 0 failing, 0 skipped
- `grep -c 'it.skip\|test.skip\|describe.skip' src/store/gameStore.turns.test.ts src/store/gameStore.error.test.ts src/store/gameStore.markets.test.ts` returns 0
</verification>

<success_criteria>
Bankruptcy removes tiles at half cost and loses VP for remaining shortfall. Empty coal market charges 8 per piece, empty iron market charges 6 per piece. Turn order is spending-based with ties preserving order. Player switching handles 2 actions per turn (1 in first round). All edge case test files have 0 failing and 0 skipped tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-04-SUMMARY.md`
</output>
