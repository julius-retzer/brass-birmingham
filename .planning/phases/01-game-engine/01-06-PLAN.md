---
phase: 01-game-engine
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/build/buildValidation.test.ts
  - src/store/shared/gameUtils.industrySlots.test.ts
autonomous: true
requirements: [ENGINE-06]
gap_closure: true

must_haves:
  truths:
    - "buildActions.ts validation functions (throwing and non-throwing) are fully tested"
    - "gameUtils.ts overbuilding logic (canOverbuildIndustry, performOverbuild) is fully tested"
    - "gameUtils.ts edge cases (getCurrentPlayer throw, validateIndustryBuildLocation, canCityAccommodateIndustryType empty) are tested"
  artifacts:
    - path: "src/store/build/buildValidation.test.ts"
      provides: "Tests for all buildActions.ts uncovered paths"
    - path: "src/store/shared/gameUtils.industrySlots.test.ts"
      provides: "Tests for gameUtils.ts uncovered paths"
  key_links:
    - from: "src/store/build/buildValidation.test.ts"
      to: "src/store/build/buildActions.ts"
      via: "import and invoke validation functions"
      pattern: "import.*from.*buildActions"
    - from: "src/store/shared/gameUtils.industrySlots.test.ts"
      to: "src/store/shared/gameUtils.ts"
      via: "import and invoke utility functions"
      pattern: "import.*from.*gameUtils"
---

<objective>
Cover all uncovered lines in buildActions.ts (lines 31-75, 87, 101, 118, 125, 138, 145, 157, 165-167, 179, 186, 192, 195, 234, 246, 271, 311, 325, 339, 352) and gameUtils.ts (lines 9, 23, 173, 220, 362, 370, 391, 420-496, 509).

Purpose: Close ENGINE-06 coverage gap for these two modules. buildActions.ts is at 67.96% (target: 100%). gameUtils.ts is at 81.96% (target: 100%).
Output: Updated test files with full coverage of all error paths, validation branches, overbuilding logic, and edge cases.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/build/buildActions.ts
@src/store/shared/gameUtils.ts
@src/store/build/buildValidation.test.ts
@src/store/shared/gameUtils.industrySlots.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover all uncovered buildActions.ts paths</name>
  <files>src/store/build/buildValidation.test.ts</files>
  <read_first>
    - src/store/build/buildActions.ts (the module under test — read fully to understand all branches)
    - src/store/build/buildValidation.test.ts (existing tests to extend)
    - src/store/gameStore.ts (for GameState type and context shapes)
    - src/data/board.ts (for CityId values)
    - src/data/industryTiles.ts (for IndustryTile shape)
  </read_first>
  <behavior>
    Throwing validation functions (lines 31-75):
    - validateBuildActionSelections: throws "No card selected" when context.selectedCard is null
    - validateBuildActionSelections: throws "No location selected" when context.selectedLocation is null
    - validateNetworkRequirement: returns without throwing for location/wild_location cards
    - validateNetworkRequirement: throws "not connected" for industry card when location not in network
    - validateIndustrySlotAvailability: throws "No industry tile selected" when selectedIndustryTile is null
    - validateIndustrySlotAvailability: throws "Cannot build...No available slots" when canCityAccommodateIndustryType returns false

    Non-throwing validation (lines 82-151):
    - validateIndustrySlotAvailabilityResult: returns {isValid: false} when no industry tile (line 87)
    - validateIndustrySlotAvailabilityResult: returns {isValid: false} when slot not available (line 101)
    - validateNetworkRequirementResult: returns {isValid: true} for location cards (line 118)
    - validateNetworkRequirementResult: returns {isValid: false} for industry card not in network (line 125)
    - validateBuildActionSelectionsResult: returns {isValid: false} when no card (line 138)
    - validateBuildActionSelectionsResult: returns {isValid: false} when no location (line 145)

    Card validation (lines 154-197):
    - validateCardType: throws for invalid card type (line 157)
    - validateCardLocationMatching: throws when location card doesn't match selected location (lines 165-167)
    - validateCardIndustryMatching: throws when industry card doesn't include tile type (line 179)
    - validateCardIndustryMatching: throws when industry card has no tile selected (line 186)
    - validateTileEraCompatibility: throws for canal-only tile in rail era (line 192)
    - validateTileEraCompatibility: throws for rail-only tile in canal era (line 195)

    buildIndustryTile function (lines 209-370):
    - throws when overbuild check fails (line 234)
    - throws when coal consumption fails (line 246)
    - throws when player cannot afford total cost (line 271)
    - coal mine auto-sells to market when connected to merchant (lines 297-313)
    - iron works auto-sells to market (lines 314-328)
    - calls performOverbuild when overbuildCheck.existingIndustry exists (line 339)
    - advances income when tile auto-flips on build (line 352)
  </behavior>
  <action>
    Add test cases to the existing `src/store/build/buildValidation.test.ts` file. Import the functions directly:

    ```typescript
    import {
      validateBuildActionSelections,
      validateNetworkRequirement,
      validateIndustrySlotAvailability,
      validateIndustrySlotAvailabilityResult,
      validateNetworkRequirementResult,
      validateBuildActionSelectionsResult,
      validateCardType,
      validateCardLocationMatching,
      validateCardIndustryMatching,
      validateTileEraCompatibility,
      buildIndustryTile,
    } from './buildActions'
    ```

    Create minimal GameState-like contexts for each test using type assertions. For `buildIndustryTile` tests, create contexts with:
    - A player with money, hand, industryTilesOnMat
    - coalMarket and ironMarket arrays
    - selectedLocation, selectedIndustryTile set
    - For coal mine auto-sell: tile.type='coal', tile.coalProduced=2, and merchants array with connected merchant
    - For iron auto-sell: tile.type='iron', tile.ironProduced=3
    - For overbuild: existing industry at same location with lower level
    - For insufficient funds: player.money less than tile cost + resource costs
    - For auto-flip income advance: tile that flips on build (coal/iron with cubes selling to market)

    Each test should directly call the function and assert the expected outcome (throw message or return value).
  </action>
  <verify>
    <automated>pnpm test --run src/store/build/buildValidation.test.ts && pnpm test --run --coverage 2>&1 | grep "buildActions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - buildValidation.test.ts contains `describe('validateBuildActionSelections'` with at least 2 test cases
    - buildValidation.test.ts contains `describe('validateNetworkRequirement'` with at least 2 test cases
    - buildValidation.test.ts contains `describe('validateIndustrySlotAvailability'` with at least 2 test cases
    - buildValidation.test.ts contains `describe('validateCardType'` with at least 1 test case
    - buildValidation.test.ts contains `describe('validateCardLocationMatching'` with at least 1 test case
    - buildValidation.test.ts contains `describe('validateCardIndustryMatching'` with at least 2 test cases
    - buildValidation.test.ts contains `describe('validateTileEraCompatibility'` with at least 2 test cases
    - buildValidation.test.ts contains `describe('buildIndustryTile'` with at least 5 test cases covering coal/iron auto-sell, overbuild, insufficient funds, income advance
    - Coverage output for buildActions.ts shows > 95% lines
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>buildActions.ts line coverage reaches 95%+ with all validation error paths, card matching, era compatibility, and buildIndustryTile resource/overbuilding/auto-sell paths tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover all uncovered gameUtils.ts paths</name>
  <files>src/store/shared/gameUtils.industrySlots.test.ts</files>
  <read_first>
    - src/store/shared/gameUtils.ts (the module under test — read fully, especially lines 1-25, 160-180, 210-230, 350-510)
    - src/store/shared/gameUtils.industrySlots.test.ts (existing tests to extend)
    - src/store/gameStore.ts (for GameState type)
    - src/data/board.ts (for connections, cityIndustrySlots)
  </read_first>
  <behavior>
    getCurrentPlayer edge case (line 9):
    - throws "Current player not found" when players array has no entry at currentPlayerIndex

    getCardDescription (line 23):
    - returns "wild industry" for wild_industry card type

    findConnectedBreweries (line 173):
    - pushes connected opponent brewery to connectedBreweries array when brewery is connected and has beer

    checkAndFlipIndustryTilesLogic (line 220):
    - shouldFlip=true for brewery with beerBarrelsOnTile=0

    validateIndustryBuildLocation (lines 362, 370):
    - returns true for location card type
    - returns result of isLocationInPlayerNetwork for industry card type
    - returns false for unknown card type (line 370)

    canOverbuildIndustry (lines 420-496):
    - returns {canOverbuild: true} when no existing industry at location (line 416)
    - returns {canOverbuild: false} when new tile level <= existing level (line 420-426)
    - returns {canOverbuild: true} when overbuilding own tile (line 429-430)
    - returns {canOverbuild: false} when overbuilding opponent non-coal/non-iron (line 435-440)
    - returns {canOverbuild: false} when overbuilding opponent coal with coal cubes on board (lines 444-458)
    - returns {canOverbuild: false} when overbuilding opponent iron with iron cubes on board (lines 459-474)
    - returns {canOverbuild: true} when overbuilding opponent coal with zero coal anywhere (line 476)

    canCityAccommodateIndustryType (line 509):
    - returns false when city has no defined slots
  </behavior>
  <action>
    Add test cases to the existing `src/store/shared/gameUtils.industrySlots.test.ts` file. Import functions:

    ```typescript
    import {
      getCurrentPlayer,
      getCardDescription,
      findConnectedBreweries,
      checkAndFlipIndustryTilesLogic,
      validateIndustryBuildLocation,
      canOverbuildIndustry,
      performOverbuild,
      canCityAccommodateIndustryType,
    } from './gameUtils'
    ```

    For `canOverbuildIndustry` tests, create GameState mocks with:
    - `players` array with industries at specific locations
    - `coalMarket` / `ironMarket` arrays with cubes counts
    - Test all 7 branches listed above

    For `findConnectedBreweries`, create context with opponent player having unflipped brewery with beer at a connected location.

    For `checkAndFlipIndustryTilesLogic`, create context with unflipped brewery at beerBarrelsOnTile=0.

    For `getCurrentPlayer`, create context with empty players array and currentPlayerIndex=5.

    For `getCardDescription`, create wild_industry card object `{ type: 'wild_industry', id: 'test' }`.

    For `validateIndustryBuildLocation`, test location card returns true, industry card checks network, and an unrecognized card type returns false.

    For `performOverbuild`, create context with existing industry to be replaced and verify the industry is removed from the target player.
  </action>
  <verify>
    <automated>pnpm test --run src/store/shared/gameUtils.industrySlots.test.ts && pnpm test --run --coverage 2>&1 | grep "gameUtils.ts"</automated>
  </verify>
  <acceptance_criteria>
    - gameUtils.industrySlots.test.ts contains `describe('getCurrentPlayer'` with test for throw case
    - gameUtils.industrySlots.test.ts contains `describe('getCardDescription'` with test for wild_industry
    - gameUtils.industrySlots.test.ts contains `describe('canOverbuildIndustry'` with at least 6 test cases
    - gameUtils.industrySlots.test.ts contains `describe('performOverbuild'` with at least 1 test case
    - gameUtils.industrySlots.test.ts contains `describe('findConnectedBreweries'` with at least 1 test for opponent breweries
    - gameUtils.industrySlots.test.ts contains `describe('validateIndustryBuildLocation'` with at least 3 test cases
    - Coverage output for gameUtils.ts shows > 95% lines
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>gameUtils.ts line coverage reaches 95%+ with all overbuilding validation paths, brewery finding, auto-flip logic, and edge cases tested</done>
</task>

</tasks>

<verification>
Run full coverage and confirm both files improved:
```bash
pnpm test --run --coverage 2>&1 | grep -E "(buildActions|gameUtils)\.ts"
```
buildActions.ts must show > 95% lines. gameUtils.ts must show > 95% lines.
</verification>

<success_criteria>
- buildActions.ts line coverage > 95% (from 67.96%)
- gameUtils.ts line coverage > 95% (from 81.96%)
- All existing 165+ tests still pass
- No new test files created (tests added to existing files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-06-SUMMARY.md`
</output>
