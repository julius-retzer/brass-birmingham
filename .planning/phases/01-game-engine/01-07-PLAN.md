---
phase: 01-game-engine
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/gameStore.sell.test.ts
  - src/store/gameStore.network.test.ts
  - src/store/gameStore.actions.test.ts
autonomous: true
requirements: [ENGINE-06]
gap_closure: true

must_haves:
  truths:
    - "Sell action merchant bonus branches (money, income, VP, develop) are fully tested via state machine"
    - "Network action error paths (missing card/link, double link coal/beer failures) are tested"
    - "Scout, pass action error/edge paths are tested"
    - "selectIndustryType and selectTilesForDevelop actions are tested"
  artifacts:
    - path: "src/store/gameStore.sell.test.ts"
      provides: "Tests for sell action merchant bonuses and beer insufficient paths"
    - path: "src/store/gameStore.network.test.ts"
      provides: "Tests for double network action error paths"
    - path: "src/store/gameStore.actions.test.ts"
      provides: "Tests for selectIndustryType, selectTilesForDevelop, scout/pass edge cases"
  key_links:
    - from: "src/store/gameStore.sell.test.ts"
      to: "src/store/gameStore.ts"
      via: "XState actor send/snapshot for sell action"
      pattern: "actor\\.send.*CONFIRM_ACTION"
    - from: "src/store/gameStore.network.test.ts"
      to: "src/store/gameStore.ts"
      via: "XState actor send/snapshot for network action"
      pattern: "actor\\.send.*SELECT_LINK"
---

<objective>
Cover uncovered lines in gameStore.ts related to action execution and selection logic: sell action merchant bonuses (lines 1285-1286, 1329-1385, 1419), network action error paths (lines 740-743, 869, 917, 954, 973), scout/pass edge cases (lines 1452, 1468, 1504, 1521), selectIndustryType (lines 1780-1833), and build action validation branches (lines 600, 621, 678, 721-722).

Purpose: Close ENGINE-06 coverage gap for gameStore.ts action execution code. These are the "middle" of the file — action handlers and selection actions.
Output: Extended test files covering all action execution error paths, merchant bonus handling, and selection actions.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/gameStore.ts
@src/store/gameStore.sell.test.ts
@src/store/gameStore.network.test.ts
@src/store/gameStore.actions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover sell action merchant bonuses and beer-insufficient paths</name>
  <files>src/store/gameStore.sell.test.ts</files>
  <read_first>
    - src/store/gameStore.ts (lines 1225-1443 — executeSellAction, especially merchant bonus switch at 1324-1387 and beer failure at 1283-1296)
    - src/store/gameStore.sell.test.ts (existing sell tests)
    - src/data/merchants.ts (merchant structure with bonusType)
    - src/store/market/marketActions.ts (consumeBeerFromSources return shape, merchantBonusesCollected)
  </read_first>
  <behavior>
    Sell action — beer insufficient (lines 1283-1296):
    - When sell action executes but beer is not available, logs error and returns no state changes

    Sell action — merchant income bonus (lines 1329-1333):
    - When merchant bonus type is 'income', updatedPlayer.income increases by bonus.value (capped at MAX_INCOME=30)

    Sell action — merchant VP bonus (lines 1334-1336):
    - When merchant bonus type is 'victoryPoints', updatedPlayer.victoryPoints increases by bonus.value

    Sell action — merchant develop bonus (lines 1337-1385):
    - When merchant bonus type is 'develop', lowest level tile is removed from player mat
    - Skips pottery tiles with hasLightbulbIcon
    - Decrements quantity of the lowest tile found

    Sell action — autoFlip after sell (line 1419):
    - When beer consumption causes a brewery to empty, that brewery auto-flips

    Build action — validation failed branch (line 600):
    - When validateBuildActionSelectionsResult/validateNetworkRequirementResult/validateIndustrySlotAvailabilityResult returns isValid=false, sets lastError

    Build action — card validation catch (line 621):
    - When validateCardType/validateCardLocationMatching/validateCardIndustryMatching throws, sets lastError

    Build action — autoFlip after build (line 678):
    - When building consumes resources causing a tile to flip, checkAndFlipIndustryTilesLogic returns updated players

    Build action — location card fallback (lines 721-722):
    - When card is location type, uses simplified build path without resource consumption tracking
  </behavior>
  <action>
    Add new describe blocks to `src/store/gameStore.sell.test.ts`:

    1. **"Sell Action - Merchant Bonuses"** describe block:
       - Test income bonus: Set up a game state where a player has an unflipped cotton/manufacturer/pottery at a location connected to a merchant with `bonusType: 'income'` and `bonusValue: 2`. Execute sell action. Assert player income increased by bonus value.
       - Test VP bonus: Same setup but merchant has `bonusType: 'victoryPoints'`, `bonusValue: 2`. Assert victoryPoints increased.
       - Test develop bonus: Merchant has `bonusType: 'develop'`. Assert lowest-level non-pottery-lightbulb tile quantity decremented on player mat.

    2. **"Sell Action - Insufficient Beer"** describe block:
       - Set up player with sellable industry at location with no breweries connected and no merchant beer. Execute sell action. Assert the state has an error log entry containing "insufficient beer".

    Use TEST_ events to set up required state: TEST_SET_PLAYER_STATE for industries/hand, then navigate to sell action flow (SELECT_ACTION 'sell', SELECT_CARD, CONFIRM_ACTION).

    Also add to `src/store/gameStore.build.test.ts` or `src/store/gameStore.actions.test.ts`:
    - Test for build validation failure path (set up build with industry card pointing to location not in network, assert lastError is set)
    - Test for location card build path (use location card, build with it, verify simplified path works)
  </action>
  <verify>
    <automated>pnpm test --run src/store/gameStore.sell.test.ts && pnpm test --run --coverage 2>&1 | grep "gameStore.ts"</automated>
  </verify>
  <acceptance_criteria>
    - gameStore.sell.test.ts contains `describe('Sell Action - Merchant Bonuses'` or similar with tests for income, VP, and develop bonus types
    - gameStore.sell.test.ts contains test for beer-insufficient sell scenario
    - At least one test verifies `merchantBonusesCollected` with `type: 'develop'` triggers tile removal
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Sell action merchant bonus paths (money, income, VP, develop) and beer-insufficient path are covered by passing tests. Lines 1285-1286, 1329-1385, 1419 in gameStore.ts are exercised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover network action error paths and selection actions</name>
  <files>src/store/gameStore.network.test.ts, src/store/gameStore.actions.test.ts</files>
  <read_first>
    - src/store/gameStore.ts (lines 737-1017 — executeNetworkAction and executeDoubleNetworkAction; lines 1779-1836 — selectIndustryType and selectTilesForDevelop; lines 1446-1540 — executeScoutAction and executePassAction)
    - src/store/gameStore.network.test.ts (existing network tests)
    - src/store/gameStore.actions.test.ts (existing action tests)
    - src/store/gameStore.scout.test.ts (existing scout tests)
    - src/store/gameStore.pass.test.ts (existing pass tests)
  </read_first>
  <behavior>
    Network action — missing card/link guard (lines 740-743):
    - When executeNetworkAction is called without selectedCard or selectedLink, logs warning and returns empty

    Double network action — missing selections (line 869):
    - When executeDoubleNetworkAction called without selectedCard/selectedLink/selectedSecondLink, throws error

    Double network action — first coal fail (line 917):
    - When first coal consumption fails, throws "Failed to consume coal for first rail link"

    Double network action — second coal fail (line 954):
    - When second coal consumption fails, throws "Failed to consume coal for second rail link"

    Double network action — beer fail (line 973):
    - When beer consumption fails for double link, throws "Beer consumption failed"

    Scout action — wrong card count (line 1452):
    - When selectedCardsForScout.length != SCOUT_CARDS_REQUIRED, throws error

    Scout action — no wild cards available (line 1468):
    - When wildLocationPile or wildIndustryPile is empty, throws "No wild cards available"

    Pass action — wild location card (line 1518-1519):
    - When passing with wild_location card, card goes to wildLocationPile (not discardPile)

    Pass action — wild industry card (lines 1520-1521):
    - When passing with wild_industry card, card goes to wildIndustryPile (not discardPile)

    selectIndustryType (lines 1780-1805):
    - Finds lowest available tile of selected industry type
    - If selected card is location/wild_location, auto-sets selectedLocation

    selectTilesForDevelop (lines 1807-1835):
    - Validates each selected industry type has developable tiles
    - Filters out pottery with lightbulb icon
    - Limits to max 2 tiles
  </behavior>
  <action>
    **In gameStore.network.test.ts**, add:
    - "Double Network Action - Error Paths" describe block testing the coal/beer failure scenarios. Use TEST_ events to set up rail era with two selected links but no coal available (empty market, no coal mines connected). This requires navigating to the double link confirmation state.

    **In gameStore.actions.test.ts**, add:
    - "selectIndustryType" describe block: Set up state machine in build flow with industry card selected, then send SELECT_INDUSTRY_TYPE event. Assert selectedIndustryTile is set to lowest available tile. Test with location card to verify auto-location-select.
    - "selectTilesForDevelop" describe block: Send SELECT_TILES_FOR_DEVELOP event with industry types. Assert selectedTilesForDevelop is set. Test pottery-with-lightbulb filtering. Test max-2-tile limit.
    - "Pass with wild cards" test: Set up player with wild_location card, execute pass with it, verify card goes to wildLocationPile. Same for wild_industry.

    Use TEST_SET_PLAYER_HAND, TEST_SET_ERA, and other TEST_ events for state setup. For double link tests, the actor must be in rail era with proper link selections made.
  </action>
  <verify>
    <automated>pnpm test --run src/store/gameStore.network.test.ts src/store/gameStore.actions.test.ts && pnpm test --run --coverage 2>&1 | grep "gameStore.ts"</automated>
  </verify>
  <acceptance_criteria>
    - gameStore.network.test.ts contains tests for double network coal/beer failure scenarios
    - gameStore.actions.test.ts contains `describe('selectIndustryType'` with at least 2 test cases
    - gameStore.actions.test.ts contains `describe('selectTilesForDevelop'` with at least 2 test cases
    - gameStore.actions.test.ts or gameStore.pass.test.ts contains tests for wild card pass routing (wildLocationPile, wildIndustryPile)
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Network action error paths, selection actions (selectIndustryType, selectTilesForDevelop), and pass wild card routing are covered by passing tests. Lines 740-743, 869, 917, 954, 973, 1452, 1468, 1504, 1521, 1780-1835 in gameStore.ts are exercised.</done>
</task>

</tasks>

<verification>
Run full coverage and check gameStore.ts improvement:
```bash
pnpm test --run --coverage 2>&1 | grep "gameStore.ts"
```
gameStore.ts line coverage should improve significantly from 82.28% toward 95%+.
</verification>

<success_criteria>
- gameStore.ts line coverage improves from 82.28% to 90%+ (action execution paths)
- All existing 165+ tests still pass plus new tests
- Merchant bonus handling (money, income, VP, develop) has test coverage
- Double network error paths have test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-07-SUMMARY.md`
</output>
