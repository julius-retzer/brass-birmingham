---
phase: 01-game-engine
plan: 08
type: execute
wave: 2
depends_on: [06, 07]
files_modified:
  - src/store/gameStore.guards.test.ts
  - src/store/gameStore.edgecases.test.ts
  - src/store/market/marketActions.test.ts
autonomous: true
requirements: [ENGINE-06, ENGINE-07]
gap_closure: true

must_haves:
  truths:
    - "All guard functions in gameStore.ts are tested (canCompleteBuild, hasSelectedLink, canBuildLink, canSelectLocation, canSelectIndustryType, isGameEnd, canBuildSecondLink, hasSelectedTilesForDevelop, canCompleteDoubleLink)"
    - "marketActions.ts opponent brewery beer consumption and coal error reporting paths are tested"
    - "No valid moves edge case is tested"
    - "Last card scenarios edge case is tested"
    - "100% line coverage achieved across gameStore.ts, buildActions.ts, gameUtils.ts"
  artifacts:
    - path: "src/store/gameStore.guards.test.ts"
      provides: "Tests for all guard functions"
    - path: "src/store/gameStore.edgecases.test.ts"
      provides: "Tests for no-valid-moves and last-card-scenarios"
    - path: "src/store/market/marketActions.test.ts"
      provides: "Tests for opponent brewery beer consumption and coal error paths"
  key_links:
    - from: "src/store/gameStore.guards.test.ts"
      to: "src/store/gameStore.ts"
      via: "XState actor send/snapshot testing guard transitions"
      pattern: "actor\\.send.*SELECT"
    - from: "src/store/gameStore.edgecases.test.ts"
      to: "src/store/gameStore.ts"
      via: "XState actor send for edge case scenarios"
      pattern: "actor\\.send.*TEST_SET"
---

<objective>
Cover remaining uncovered lines in gameStore.ts guards (lines 2221, 2232, 2264, 2283, 2294, 2307, 2323, 2367, 2372-2373, 2381-2418, 2437-2440, 2473, 2500, 2510), marketActions.ts gaps (lines 67, 112, 123, 128, 134, 211, 470-488, 547, 551), industryTiles.ts utility functions (lines 632-648, 690-698, 722-726), remaining gameStore.ts lines (345, 483, 519, 534-542, 1901-1902, 1915-1918), and ENGINE-07 edge cases (no valid moves, last card scenarios).

Purpose: Final gap closure plan to reach 100% coverage on the game engine and complete ENGINE-07.
Output: New test files for guards, edge cases, and market actions. Full 100% coverage on game engine modules.
</objective>

<execution_context>
@/Users/juliusretzer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusretzer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/store/gameStore.ts
@src/store/market/marketActions.ts
@src/data/industryTiles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover guard functions and remaining gameStore.ts lines</name>
  <files>src/store/gameStore.guards.test.ts</files>
  <read_first>
    - src/store/gameStore.ts (lines 2215-2530 — all guard functions; lines 330-345 — merchant setup for 4 players; lines 499-519 — selectCardForScout; lines 534-542 — updatePlayer2Name/JOIN_GAME; lines 1900-1918 — setEraEndConditions, trackMoneySpent)
    - src/store/gameStore.xstate.test.ts (existing XState tests for patterns)
    - src/store/gameStore.actions.test.ts (for action flow patterns)
    - src/data/board.ts (connections array for canBuildLink tests)
  </read_first>
  <behavior>
    canCompleteBuild guard (lines 2221, 2232):
    - Returns true when location card + selectedCard + selectedLocation are set
    - Returns false when industry card but selectedIndustryTile is null
    - Returns false when coal is required but not available

    hasSelectedLink guard (line 2264):
    - Returns false when selectedLink is null
    - Returns false in rail era when no coal available for the link location

    canBuildLink guard (lines 2283, 2294, 2307, 2323):
    - Returns false for non-SELECT_LINK event types
    - Returns false when connection doesn't exist in board data
    - Returns false when link already built by any player
    - Returns false for SELECT_SECOND_LINK when no first link selected

    canSelectLocation guard (lines 2367, 2372-2373):
    - Returns false when build validation fails
    - Checks location card matching (locationCard.location === event.cityId)

    canSelectIndustryType guard (lines 2381-2418):
    - Returns false when no card selected
    - Returns false when no tiles of that industry type available for current era
    - For location cards, checks canCityAccommodateIndustryType
    - For wild_location cards, returns true (no slot restriction)

    isGameEnd guard (lines 2437-2440):
    - Returns true when era='rail' AND drawPile empty AND all hands empty
    - Returns false when era='canal' even if deck/hands empty

    canBuildSecondLink guard (line 2473):
    - Checks own and opponent brewery beer availability

    hasSelectedTilesForDevelop guard (line 2500):
    - Returns false when no tiles selected and no developable tiles exist

    canCompleteDoubleLink guard (line 2510):
    - Returns false when selectedCard/selectedLink/selectedSecondLink is null or era is not rail

    Remaining lines:
    - Line 345: playerCount >= 4 merchant branch (test with 4-player setup)
    - Line 483: era filter `return false` in selectCardForBuild (card type not matching canal/rail)
    - Line 519: selectCardForScout returns {} when card already selected or max reached
    - Lines 534-542: JOIN_GAME event updates player 2 name
    - Lines 1901-1902: TEST_SET_ERA_END_CONDITIONS event handler
    - Lines 1915-1918: trackMoneySpent action
  </behavior>
  <action>
    Create `src/store/gameStore.guards.test.ts` with:

    1. **"Guard: canCompleteBuild"**: Create actor, navigate to build flow. Test with location card (card + location = true). Test with industry card but no tile (false). Test with industry card needing coal but none available (false).

    2. **"Guard: hasSelectedLink"**: Create actor in network flow. Test with selectedLink null (can't confirm). Test in rail era with no coal (can't confirm).

    3. **"Guard: canBuildLink"**: Create actor. Test with non-existent connection (birmingham to derby — no direct connection). Test with already-built link. Test SELECT_SECOND_LINK with no first link. Use TEST_SET_ERA for rail era tests.

    4. **"Guard: canSelectLocation"**: Navigate to build flow with industry card. Send SELECT_LOCATION for city not in network (should fail guard).

    5. **"Guard: canSelectIndustryType"**: Navigate to build flow. Test with no tiles available for selected type. Test with location card checking slot accommodation.

    6. **"Guard: isGameEnd"**: Use TEST_SET_ERA to set canal era, empty deck/hands — should NOT be game end. Set rail era, empty deck/hands — IS game end.

    7. **"Guard: hasSelectedTilesForDevelop"**: Set up player with no developable tiles (only pottery with lightbulb). Should return false.

    8. **"Guard: canCompleteDoubleLink"**: Test returns false when era is canal.

    9. **"JOIN_GAME event"**: Send JOIN_GAME with player2Name, verify players[1].name updated.

    10. **"selectCardForScout edge"**: Select 3 cards for scout, try selecting a 4th — should be ignored.

    11. **"TEST_SET_ERA_END_CONDITIONS"**: Send event, verify drawPile updated.

    Use `createActor` from xstate, `send` events, and `getSnapshot` to verify guard effects via state transitions (guards that block will leave machine in same state).
  </action>
  <verify>
    <automated>pnpm test --run src/store/gameStore.guards.test.ts && pnpm test --run --coverage 2>&1 | grep "gameStore.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File src/store/gameStore.guards.test.ts exists
    - Contains at least 10 describe blocks or test cases covering the guard functions listed
    - Contains test for JOIN_GAME event updating player 2 name
    - Contains test for canSelectIndustryType with location card
    - Contains test for isGameEnd returning false in canal era
    - Contains test for TEST_SET_ERA_END_CONDITIONS event
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>All guard functions in gameStore.ts have test coverage. JOIN_GAME, selectCardForScout edge, and TEST_ event handlers are tested. Lines 345, 483, 519, 534-542, 1901-1902, 1915-1918, 2221, 2232, 2264, 2283, 2294, 2307, 2323, 2367, 2372-2373, 2381-2418, 2437-2440, 2473, 2500, 2510 are exercised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover marketActions.ts gaps, industryTiles.ts utilities, and ENGINE-07 edge cases</name>
  <files>src/store/market/marketActions.test.ts, src/store/gameStore.edgecases.test.ts</files>
  <read_first>
    - src/store/market/marketActions.ts (lines 67, 112, 123, 128, 134, 211, 470-488, 547, 551 — coal error reporting, opponent brewery beer, autoflip logs)
    - src/store/gameStore.markets.test.ts (existing market tests for patterns)
    - src/data/industryTiles.ts (lines 628-728 — getInitialPlayerIndustryTiles, getLowestAvailableTile, canBuildTileInEra, canDevelopTile)
    - src/data/industryTiles.test.ts (existing tests)
    - src/store/gameStore.ts (for edge case flows — no valid moves and last card scenarios)
    - ai-docs/brass-birmingham-rules.mdc (for edge case rules reference)
  </read_first>
  <behavior>
    marketActions.ts:
    - Coal error reporting (line 112): coal loop exits when no coal found in market AND not connected
    - Coal error with connected mines exhausted (line 123): error message includes "connected coal mines (exhausted)"
    - Coal error with market connection (line 128): error message includes "coal markets"
    - Coal error no sources (line 134): error message includes "No coal mines or market connection"
    - Iron consumption detail (line 211): multi-cube iron from iron works log
    - Opponent brewery beer consumption (lines 470-488): beer consumed from opponent's connected brewery, their barrel count decremented
    - AutoFlip logs after beer consumption (lines 547, 551): when beer consumption causes flip, log messages included

    industryTiles.ts:
    - getInitialPlayerIndustryTiles (lines 632-648): returns record with all 6 industry types populated
    - getLowestAvailableTile (lines 690-698): returns lowest level tile with quantity > 0, or null if none
    - canBuildTileInEra (lines 722): returns canBuildInCanalEra for canal, canBuildInRailEra for rail
    - canDevelopTile (line 726): returns !hasLightbulbIcon

    ENGINE-07 edge cases:
    - No valid moves: When a player has cards but cannot legally perform any action (all locations occupied, no network for industry cards, cannot afford any build, no sellable industries). Player must still play a pass action.
    - Last card scenario: When a player is on their last card and plays it, their hand becomes empty. If deck is also empty, era end triggers after opponent finishes.
  </behavior>
  <action>
    **Create `src/store/market/marketActions.test.ts`** with:

    1. **"consumeCoalFromSources - error reporting"**: Test coal consumption from location with no connected mines and no market connection. Assert success=false and logDetails contain "No coal mines or market connection". Test with connected mines but all exhausted and market connected — assert message contains "coal markets". Test with connected mines exhausted but no market — assert message contains "connected coal mines (exhausted)".

    2. **"consumeBeerFromSources - opponent brewery"**: Create context with current player at a location, opponent player has unflipped brewery with 2 beer at connected location. Call consumeBeerFromSources. Assert success=true, opponent brewery beerBarrelsOnTile decremented by 1, logDetails contains "connected opponent brewery".

    3. **"consumeBeerFromSources - autoFlip logs"**: Create context where beer consumption causes a brewery to empty (beerBarrelsOnTile goes to 0). Assert the returned logDetails include auto-flip log messages.

    Import directly: `import { consumeCoalFromSources, consumeBeerFromSources } from './marketActions'`

    **Add to `src/data/industryTiles.test.ts`** (or create new test block):
    - Test `getInitialPlayerIndustryTiles()` returns all 6 industry types with arrays
    - Test `getLowestAvailableTile([])` returns null
    - Test `getLowestAvailableTile` with mixed quantities returns lowest with qty > 0
    - Test `canBuildTileInEra(tile, 'canal')` and `canBuildTileInEra(tile, 'rail')`
    - Test `canDevelopTile(tile)` with hasLightbulbIcon=true returns false

    **Create `src/store/gameStore.edgecases.test.ts`** with:

    1. **"Edge Case: No Valid Moves"**: Set up game where player has only industry cards, no industries or links on board (so no network), and cannot afford any build. Player should still be able to execute pass action. Assert player can navigate to pass flow and discard a card.

    2. **"Edge Case: Last Card Triggers Era End"**: Set up game where draw pile is empty and one player has exactly 1 card left. That player plays their last card (pass action). After opponent plays their remaining actions, verify era scoring triggers automatically via the isEraEnd guard.
  </action>
  <verify>
    <automated>pnpm test --run src/store/market/marketActions.test.ts src/store/gameStore.edgecases.test.ts src/data/industryTiles.test.ts && pnpm test --run --coverage 2>&1 | grep -E "(gameStore|buildActions|gameUtils|marketActions|industryTiles)\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File src/store/market/marketActions.test.ts exists with at least 4 test cases
    - File src/store/gameStore.edgecases.test.ts exists with at least 2 test cases
    - marketActions.test.ts contains test for opponent brewery beer consumption
    - marketActions.test.ts contains test for coal error reporting with "No coal mines or market connection"
    - gameStore.edgecases.test.ts contains test named or described with "No Valid Moves"
    - gameStore.edgecases.test.ts contains test named or described with "Last Card"
    - industryTiles.test.ts contains tests for getInitialPlayerIndustryTiles, getLowestAvailableTile, canBuildTileInEra, canDevelopTile
    - Coverage output for marketActions.ts shows > 95% lines
    - Coverage output for industryTiles.ts shows > 80% lines (data definitions are partially covered by other tests)
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>marketActions.ts opponent brewery and coal error paths tested. industryTiles.ts utility functions tested. ENGINE-07 "no valid moves" and "last card" edge cases have explicit tests. All game engine modules at 95%+ line coverage.</done>
</task>

</tasks>

<verification>
Run full test suite with coverage:
```bash
pnpm test --run --coverage 2>&1 | grep -E "(gameStore|buildActions|gameUtils|marketActions|industryTiles)\.ts"
```

All five files must show > 95% line coverage (gameStore.ts, buildActions.ts, gameUtils.ts, marketActions.ts).
industryTiles.ts must show > 80% line coverage.

Verify ENGINE-07 edge cases explicitly:
```bash
pnpm test --run src/store/gameStore.edgecases.test.ts
```
</verification>

<success_criteria>
- gameStore.ts line coverage > 95% (from 82.28%)
- buildActions.ts line coverage > 95% (from 67.96%)
- gameUtils.ts line coverage > 95% (from 81.96%)
- marketActions.ts line coverage > 95% (from 91.89%)
- "No valid moves" edge case has passing test
- "Last card scenario" edge case has passing test
- All tests pass (190+ total expected)
- ENGINE-06 and ENGINE-07 requirements fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-08-SUMMARY.md`
</output>
